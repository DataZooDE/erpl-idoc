#
# Build & test the erpl_idoc DuckDB extension across platforms and DuckDB versions.
# Distribution to end users is via the DuckDB Community Extensions repo
# (extensions/erpl_idoc/description.yml), so this pipeline builds + tests only.
#
name: Main Extension Distribution Pipeline
on:
  push:
    paths-ignore:
      - '**/README.md'
      - 'assets/**'
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.head_ref || '' }}-${{ github.base_ref || '' }}-${{ github.ref != 'refs/heads/main' && github.sha || '' }}
  cancel-in-progress: true

jobs:
  # Fast, DuckDB-free unit tests for the pure IDoc format core (no SAP, no vcpkg).
  core-tests:
    name: Format core unit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Compile and run format-core tests
        run: make core_tests_format

  # Build + run the SQL test suite for the current DuckDB stable (v1.5.4).
  duckdb-v154-build:
    name: Build & test (v1.5.4)
    uses: duckdb/extension-ci-tools/.github/workflows/_extension_distribution.yml@v1.5-variegata
    with:
      duckdb_version: v1.5.4
      ci_tools_version: v1.5-variegata
      extension_name: erpl_idoc
      vcpkg_commit: 84bab45d415d22042bd0b9081aea57f362da3f35
      exclude_archs: 'windows_amd64_rtools;windows_amd64_mingw;wasm_mvp;wasm_eh;wasm_threads;'

  # Also build against the previous stable (v1.4.5) — track the latest-2 versions.
  duckdb-v145-build:
    name: Build & test (v1.4.5)
    uses: duckdb/extension-ci-tools/.github/workflows/_extension_distribution.yml@v1.4-andium
    with:
      duckdb_version: v1.4.5
      ci_tools_version: v1.4-andium
      extension_name: erpl_idoc
      vcpkg_commit: ce613c41372b23b1f51333815feb3edd87ef8a8b
      exclude_archs: 'windows_amd64_rtools;windows_amd64_mingw;wasm_mvp;wasm_eh;wasm_threads;'

  code-quality-check:
    name: Code Quality Check
    uses: duckdb/extension-ci-tools/.github/workflows/_extension_code_quality.yml@v1.5-variegata
    with:
      duckdb_version: v1.5.4
      ci_tools_version: v1.5-variegata
      extension_name: erpl_idoc
      format_checks: 'format'
