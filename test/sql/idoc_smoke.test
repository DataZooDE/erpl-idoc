# name: test/sql/idoc_smoke.test
# description: erpl_idoc loads and registers its entry function
# group: [erpl_idoc]

# Before we load the extension, this fails
statement error
SELECT sap_idoc_version('hello');
----
Catalog Error: Scalar Function with name sap_idoc_version does not exist!

require erpl_idoc

query I
SELECT sap_idoc_version('loaded');
----
erpl_idoc loaded
